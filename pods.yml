apiVersion: v1
kind: Pod
metadata:
  name: node-app
  labels:
    app: node-app
spec:
  containers:
    - name: node-app
      image: dmedema/nodeapp:tagVersion
      ports:
        - containerPort: 8080
